const foo = () => {
  console.log("arrow function");
};

// function foo() {
//   console.log("aroow funcion");
// }
// 둘은 동일한 함수

const foo2 = (x, y) => x + y;
// const foo2 = (x, y) => {
//   return x + y;
// }; 둘도 동일한 함수
